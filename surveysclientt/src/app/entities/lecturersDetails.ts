import { logging } from 'protractor';

export interface LecturersDetailsDTO {
    idLecturers: number;
    name: string;
    surname: string;
    phone: string;
    email: string;
    title: string;
}
